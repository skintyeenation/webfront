packages:
  - app
  - api
  - packages/*
  - lookup/api
  - lookup/app
  # website/ itself is a WordPress + Docker project (PHP/Python tooling), not an
  # npm package — only its Next.js frontend (website/web) is a workspace member,
  # so it can consume the shared @skintyee/api-client + @skintyee/models packages.
  - website/web
