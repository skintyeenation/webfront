packages:
  - app
  - api
  - packages/*
  - lookup/api
  - lookup/app
  # website/ is a WordPress + Docker project (PHP/Python tooling), not an npm
  # package, so it is intentionally NOT a workspace member.
