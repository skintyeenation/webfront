# Expo SDK 48 webpack expects React Native style nested node_modules layout.
# pnpm's default symlinked structure breaks source-map-loader when it tries
# to resolve transitive deps (e.g. react-is inside react-redux). Hoist
# everything to keep webpack happy.
node-linker=hoisted
shamefully-hoist=true
