// Moved to the shared workspace package @skintyee/models (so the website reuses
// the same domain types). Re-exported here so the app's existing
// `skintyee/models` imports keep working unchanged.
export * from '@skintyee/models';
