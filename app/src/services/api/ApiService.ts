// Moved to the shared workspace package @skintyee/api-client (so the website
// reuses the same ApiService contract). Re-exported here so the app's existing
// `skintyee/services/api/ApiService` imports keep working unchanged.
export * from '@skintyee/api-client';
