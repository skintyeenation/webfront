# React Native / Expo + webpack expect a flat (hoisted) node_modules. pnpm's
# default isolated layout breaks resolution of transitive deps like
# @babel/runtime and use-sync-external-store under the webpack/metro bundlers,
# so use the hoisted node-linker for this workspace.
node-linker=hoisted
